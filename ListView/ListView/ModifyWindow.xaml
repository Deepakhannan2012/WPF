<Window x:Class="ListView.ModifyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListView"
        mc:Ignorable="d"
        Title="ModifyWindow" SizeToContent="WidthAndHeight">
    <StackPanel>
    <Grid Margin="10,10,10,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
            <Grid.Resources>
                <Style TargetType="TextBox">
                    <Setter Property="MinWidth" Value="200"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="0,2,0,2"/>
                </Style>
            </Grid.Resources>
        <Label Content="Emp ID:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox x:Name="empID" Grid.Row="0" Grid.Column="1"/>
        <Label Content="First Name:" Grid.Row="1"/>
        <TextBox x:Name="fstName" Grid.Row="1" Grid.Column="1"/>
        <Label Content="Last Name:" Grid.Row="2"/>
        <TextBox x:Name="LstName" Grid.Row="2" Grid.Column="1"/>
        <Label Content="Date of Birth:" Grid.Row="3"/>
        <TextBox x:Name="DOB" Grid.Row="3" Grid.Column="1"/>
        <Label Content="E-mail:" Grid.Row="4"/>
        <TextBox x:Name="EMail" Grid.Row="4" Grid.Column="1"/>
        <Label Content="Contact No.:" Grid.Row="5"/>
        <TextBox x:Name="ConNum" Grid.Row="5" Grid.Column="1"/>
    </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="MinWidth" Value="50"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
            </StackPanel.Resources>
            <Button Content="Add" MinWidth="50" Margin="0,5,5,10"/>
            <Button Content="Cancel" MinWidth="50" Margin="0,5,10,10"/>
        </StackPanel>
    </StackPanel>
</Window>
